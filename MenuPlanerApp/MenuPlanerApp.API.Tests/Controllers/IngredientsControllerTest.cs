using NUnit.Framework;

namespace MenuPlanerApp.API.Tests.Controllers
{
    [TestFixture]
    internal class IngredientsControllerTest
    {
        [Test]
        public void GetReturnsIngredients()
        {
            /*
            // Arrange
            var mockOptions = new Mock<DbContextOptions>();
            var mockContext = new Mock<MenuPlanerAppApiContext>(mockOptions).Object;
                var controller = new IngredientsController(mockContext);

            // Act
            var response = controller.GetIngredient(2);

            // Assert
            Assert.That(response.Result.Value.Id.Equals(2));
            */
        }
    }
}